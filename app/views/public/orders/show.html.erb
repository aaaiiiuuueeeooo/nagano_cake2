<div>注文履歴詳細</div>


<div>注文情報</div>
<div>注文日</div> <div><%= @order.created_at %></div>
<div>配送先</div> <div><%= @order.postal_code %><%= @order.address %><%= @order.name %></div>
<div>支払方法</div> <div><%= @order.payment_method %></div>
<div>ステータス</div> <div><%= @order.status %></div>

<div>請求情報</div>
<div>商品合計</div> <div><%= @order.total_payment %></div>
<div>配送料</div> <div>800</div>
<div>ご請求額</div> <div><%= @order.total_payment + 800 %></div>

<div class="table">
  <tr>
    <td>商品</td>
    <td>単価（税込）</td>
    <td>個数</td>
    <td>小計</td>
  </tr>
  <% @order_details.each do |order_detail| %>
  <tr>
    <td><%= order_detail.item.name %></td>
    <td><%= order_detail.with_tax_price %></td>
    <td><%= order_detail.amount %></td>
    <td><%= order_detail.subtotal %></td>
  </tr>
  <% end %>
</div>
